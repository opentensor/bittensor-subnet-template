How to run under windows11 + WSL
================================
1) run local-environment.yaml  with docker compose
2) open terminal, enter WSL by typing ```wsl``` and run ```./setup.sh```
3) from same terminal (WSL) run ```./run_indexer.sh```

